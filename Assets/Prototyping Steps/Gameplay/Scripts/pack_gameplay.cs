using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pack_gameplay : MonoBehaviour
{
    public Pack pack;
    public string type;
    // Start is called before the first frame update
    void Start()
    {
        type = "pack";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
